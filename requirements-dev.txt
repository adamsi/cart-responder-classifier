# Extra packages for the training pipeline (make data / train / evaluate / baseline).
-r requirements.txt
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.2
scanpy>=1.10
anndata>=0.10
h5py>=3.10
pyyaml>=6
huggingface_hub>=0.30
