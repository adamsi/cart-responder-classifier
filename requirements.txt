# Runtime for the demo app (what Render / Spaces install). No PyTorch needed: inference is numpy.
gradio>=6,<7
scikit-learn==1.9.1     # must match the version that pickled artifacts/pca.pkl
pandas>=2.2
numpy>=1.26
